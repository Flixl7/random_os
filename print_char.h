#ifndef PRINT_CHAR_H
#define PRINT_CHAR_H

// Print a single character at the current cursor position
void print_char(char c);

// Reset the cursor position to the start (e.g., after clearing line)
void reset_print_pos(void);

#endif 
